{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core invoke permissions for the main window plus updater, process, deep-link, and project-folder dialog access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "deep-link:default",
    "dialog:allow-open"
  ]
}
